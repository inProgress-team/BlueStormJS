NodeFramework
=============

## INSTALL

You need NodeJS 0.11.9

    $ (install NVM)
    $ nvm install 0.11.9
    $ nvm alias default 0.11.9
    $ nvm use default
    $ npm install # install dependencies




